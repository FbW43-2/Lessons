// arrays: square brackets

var a1 = [1, 2, 3, 4];

// objects: curly braces

var o1 = { 
    property_name: "property_value",    // this is a property of an object
    address: "Sonnenallee 11111"        // this is also a property of an object
};


Math.min(1, 2, 3, 4); // min is the function name       1, 2, 3, 4 are parameters


// these are both variables
var snakeName = "Snakky";
let snakeAge = 1;

const PI = 3.14 // this is a constant, and I'm writing it uppercase as per convention


// this is a function definition
function sum(x, y) {   // x and y are function parameters
    return x + y; // here I'm returning a value from my function
}

// here I'm calling the function
var s = sum(4, 5);

// code blocks also have curly braces
if (s >= 14) { // with this curly brace here I start a code block
    // ...
    // ...
    // ...
} // and here I finish it