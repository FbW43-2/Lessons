import React from 'react';
import BarTop from './headerBars/barTop';

const PageHeader = () => {
    return (
        <header id="header">
            <BarTop />
        </header>
    );
};

export default PageHeader;